export function getGenerateCreditFeaturesPrompt(): string {
  return `Generate real feature variables from a local credit dataset (ratios, interactions, encodings).
Use when the user asks to analyze a dataset and generate credit feature indicators.`
}
