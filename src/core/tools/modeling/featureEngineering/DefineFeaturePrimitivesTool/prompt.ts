export function getDefineFeaturePrimitivesPrompt(): string {
  return `Load and customize feature primitives from feature_primitives.yaml.

Use this to override objects, dimensions, events, or time windows before generating feature candidates.`
}
